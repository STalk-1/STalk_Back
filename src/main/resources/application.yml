springdoc:
    api-docs:
        enabled: true
    swagger-ui:
        enabled: true

server:
    port: 8080

spring:
    application:
        name: stalk-api

# dev??? ???? CORS ??(??? ?? ??? ????? ??rl)
app:
    cors:
        allowed-origins:
            - "*"
            # ??? ??? ???? ??? ?? (?: https://stalk-front.vercel.app)